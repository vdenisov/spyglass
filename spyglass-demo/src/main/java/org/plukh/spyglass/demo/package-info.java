/**
 * A runnable Spring Boot showcase for the explorer: toy endpoints that exercise the front-end features a
 * real service's spec may not contain.
 *
 * <p>The package is {@link org.jspecify.annotations.NullMarked}: every type usage is non-null unless
 * explicitly annotated {@link org.jspecify.annotations.Nullable}. NullAway enforces this at compile time
 * (see the parent pom).
 */
@NullMarked
package org.plukh.spyglass.demo;

import org.jspecify.annotations.NullMarked;
