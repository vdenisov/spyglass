/**
 * The reactive (Spring WebFlux) adapter: the single entry point and the friendly-redirect / asset-serving
 * wiring.
 *
 * <p>The package is {@link org.jspecify.annotations.NullMarked}: every type usage is non-null unless
 * explicitly annotated {@link org.jspecify.annotations.Nullable}. NullAway enforces this at compile time
 * (see the parent pom).
 */
@NullMarked
package org.plukh.spyglass.spring.webflux;

import org.jspecify.annotations.NullMarked;
